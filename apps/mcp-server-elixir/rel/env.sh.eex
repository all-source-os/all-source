#!/bin/sh
# MCP stdio server: a single foreground process, never a distributed node.
# Disable distribution so a second instance (Claude Code reload, or a leftover
# node) doesn't crash at boot with "name <node> already in use" — which closes
# stdio and surfaces as MCP -32000 Connection closed.
export RELEASE_DISTRIBUTION=none
